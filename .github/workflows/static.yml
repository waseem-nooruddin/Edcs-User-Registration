# Simple workflow for deploying static content to GitHub Pages
# NOTE: This workflow is disabled. The Playwright HTML report is now
# deployed to GitHub Pages directly from playwright.yml after each test run.
# Keeping this file for reference only.
#
# name: Deploy static content to Pages
# on:
#   push:
#     branches: ["main"]
#   workflow_dispatch:
